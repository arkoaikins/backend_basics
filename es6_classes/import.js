import { Animal } from './defClass.js';
let cat = new Animal();
console.log(cat);
//run it `npm run dev import.js`
//so now we just made cat as an object of the class animal and passed nothing to it
//so u get "Animal(head:undefined, tail:undefined"


//now move to `addParam.js`