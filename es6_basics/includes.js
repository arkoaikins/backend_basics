//the include keyword in js
let numArray = [1,2,3,4,5]

console.log(numArray.includes(7))

// and also the push keyword add a value to an array
numArray.push(7)
console.log(numArray.includes(7))
//so you could see that it is not true

// so [] is for arrays
// and {} is for objects in JS  